import React from 'react';

/**
 * @function MenuItem A helper functional component for giving options for select
 * @param {String} children A child string
 * @param {Boolean} dark If specified a dark version will be rendered
 * @returns A select option
 */


function MenuItem({ children, dark }) {
    return (
        <div>MenuItem</div>
    )
}

export default MenuItem