import React from 'react'

/**
 * @function Date renders a date
 * @param {Boolean} dark If specified a dark version will be rendered
 */

function Date({ dark }) {
  return (
    <div>Date</div>
  )
}

export default Date